apiVersion: batch/v1
kind: Job
metadata:
  name: backend-migration-job
  namespace: api-ia-web
spec:
  backoffLimit: 10
  template:
    spec:
      restartPolicy: OnFailure
      containers:
        - name: backend-migration
          image: torikano/api-ia-web-backend:1.0.0
          imagePullPolicy: IfNotPresent
          command: ["npm", "run", "migrate"]
          envFrom:
            - configMapRef:
                name: backend-config
            - secretRef:
                name: backend-secret
